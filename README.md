# data-oracle

This is a simple smart contract that allows for data to be posted on
the blockchain.  It is intended for the calculation of TVL on
defilama.

# To compile (requires node 22)

npm install
npx hardhat compile
npx hardhat test

# Also there is a docker image that runs the regression tests

docker compose build
docker compose up

